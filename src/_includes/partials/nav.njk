<nav aria-label="Primary">
  <div class="nav-inner">
    <a class="nav-brand" href="/index.html">SR-177 / Weber</a>
    {% for item in nav %}
      <a href="{{ item.url }}"{% if item.key == navKey %} class="active" aria-current="page"{% endif %}>{{ item.label }}</a>
    {% endfor %}
  </div>
</nav>
